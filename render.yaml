services:
  - type: web
    name: whisper-stt-api
    runtime: python
    plan: starter  # <--- CHANGE THIS LINE
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
